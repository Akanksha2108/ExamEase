import 'package:flutter/material.dart';

class CompPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Comp Page'),
      ),
      body: Center(
        child: Text('This is the CompPage'),
      ),
    );
  }
}