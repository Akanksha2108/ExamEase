import 'package:flutter/material.dart';

class ITPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('IT Page'),
      ),
      body: Center(
        child: Text('This is the IT Page'),
      ),
    );
  }
}